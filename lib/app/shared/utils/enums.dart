enum MessageSender { me, boss }

enum ChatMessageType { text, photo, voiceNote }

enum User { admin, staff }

enum ToastType { success, info, error }

enum ApiResponseType { success, failed, server, exception }
